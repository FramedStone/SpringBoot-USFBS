import React from "react";
import { useNavigate } from "react-router-dom";

function Navbar({ web3auth, setUser }) {
  const navigate = useNavigate();

  return (
    <nav>
      {/* ...other links... */}
    </nav>
  );
}

export default Navbar;